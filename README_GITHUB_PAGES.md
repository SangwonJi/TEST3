# GitHub Pages 배포 가이드

이 저장소의 시각화를 GitHub Pages에서 상시로 볼 수 있도록 설정하는 방법입니다.

## 🚀 빠른 시작

### 1. GitHub 저장소 준비

1. GitHub에 새 저장소를 생성하거나 기존 저장소를 사용합니다
2. 이 파일들을 저장소에 푸시합니다:
   - `index.html`
   - `_config.yml`
   - `README.md` (선택사항)

### 2. GitHub Pages 활성화

1. GitHub 저장소 페이지로 이동
2. **Settings** 탭 클릭
3. 왼쪽 메뉴에서 **Pages** 선택
4. **Source** 섹션에서:
   - **Branch**: `main` (또는 `master`) 선택
   - **Folder**: `/ (root)` 선택
5. **Save** 클릭

### 3. 접속 확인

몇 분 후 다음 URL로 접속할 수 있습니다:
```
https://[사용자명].github.io/[저장소명]/
```

예: `https://sangwonji.github.io/pubgm-visualization/`

## 📁 파일 구조

```
저장소/
├── index.html          # 메인 시각화 페이지
├── _config.yml         # Jekyll 설정 (GitHub Pages용)
├── README.md           # 프로젝트 설명
└── data/               # (선택사항) 샘플 CSV 파일
    └── sample_data.csv
```

## 🎨 기능

- **인터랙티브 트리맵**: Plotly.js를 사용한 동적 시각화
- **CSV 파일 업로드**: 브라우저에서 직접 CSV 파일을 업로드하여 시각화
- **실시간 통계**: 총 매출액, 평균 변화율, 최대/최소 변화율 표시
- **반응형 디자인**: 모바일, 태블릿, 데스크톱 모두 지원

## 📊 CSV 파일 형식

CSV 파일은 다음 형식을 따라야 합니다:

```csv
Country,Day1,Day2,Day3
USA,500000,520000,575000
China,400000,410000,434000
India,300000,320000,366000
...
```

- **Country**: 국가명
- **Day1, Day2, Day3**: 각 날짜의 매출액 (숫자)

## 🔄 자동 업데이트 (선택사항)

GitHub Actions를 사용하여 주기적으로 데이터를 업데이트하고 시각화를 재생성할 수 있습니다.

`.github/workflows/update-visualization.yml` 파일을 생성:

```yaml
name: Update Visualization

on:
  schedule:
    - cron: '0 0 * * *'  # 매일 자정 실행
  workflow_dispatch:  # 수동 실행 가능

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Update data
        run: |
          # 데이터 업데이트 스크립트 실행
          # 예: API에서 최신 데이터 가져오기
      - name: Commit changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git commit -m "Update visualization data" || exit 0
          git push
```

## 🛠️ 커스터마이징

### 색상 변경

`index.html`의 색상 매핑 부분을 수정:

```javascript
// Red-Yellow-Green 색상 맵
if (normalized < 0.5) {
    const t = normalized * 2;
    return `rgb(${255}, ${Math.round(255 * t)}, 0)`;
} else {
    const t = (normalized - 0.5) * 2;
    return `rgb(${Math.round(255 * (1 - t))}, 255, 0)`;
}
```

### 상위 국가 수 변경

`calculateStats` 함수에서 `.slice(0, 15)` 부분을 수정:

```javascript
.slice(0, 20)  // 상위 20개국으로 변경
```

## 📝 참고사항

- GitHub Pages는 정적 사이트만 호스팅 가능합니다
- Python 스크립트는 서버에서 실행할 수 없으므로 JavaScript로 변환했습니다
- CSV 파일은 클라이언트 측에서만 처리되며 서버에 업로드되지 않습니다
- 대용량 데이터(수백 MB 이상)는 브라우저 성능에 영향을 줄 수 있습니다

## 🐛 문제 해결

### 페이지가 표시되지 않음
- GitHub Pages 설정이 올바른지 확인
- `_config.yml` 파일이 있는지 확인
- 저장소가 Public인지 확인 (무료 계정의 경우)

### CSV 파일이 로드되지 않음
- CSV 파일 형식이 올바른지 확인
- 브라우저 콘솔에서 오류 메시지 확인 (F12)

### 시각화가 표시되지 않음
- 인터넷 연결 확인 (Plotly.js CDN 로드 필요)
- 브라우저 콘솔에서 JavaScript 오류 확인

## 📞 지원

문제가 발생하면 GitHub Issues에 문의해주세요.
